name: Argo_Nov_24 #let's stick with the project_month_year naming scheme
channels:
  - conda-forge
dependencies:
  # These packages are required if you want your environment to show up in jupyterlab
  - python
  - ipywidgets
  - ipykernel
  # Commonly used packages
  - xarray < 2024.3.0
  - numpy
  - scipy
  - dask
  - netcdf4
  - zarr
  - pandas
  - matplotlib
  - argopy >= 1.0.0
  - xrft
  - cartopy
  - cmocean
  - glidertools
  - seaborn
  - xhistogram