name: Argo_old_xgcm
channels:
  - conda-forge

dependencies:
  # Core
  - python=3.11
  - pip
  - ipykernel
  - ipywidgets

  # Scientific stack
  - numpy
  - scipy
  - pandas
  - dask
  - xarray<2024.3.0
  - zarr

  # IO + formats
  - netcdf4
  - h5py
  - h5netcdf

  # Visualization
  - matplotlib
  - seaborn
  - cmocean
  - cartopy

  # Analysis tools
  - xrft
  - xhistogram
  - flox
  - numba
  - gcm_filters
  - graphviz
  - kneed

  # Domain-specific
  - glidertools
  - xgcm=0.6.1

  # Pip-only packages (if any)
  - pip:
      # Add pip-only packages here if needed
      []